{
  "name": "gerenciamento-lotes-ml",
  "version": "1.0.0",
  "description": "Sistema de conferência de lotes do Mercado Livre",
  "private": true,
  "scripts": {
    "dev": "cross-env VITE_SAFE_MODE=0 vite",
    "dev:safe": "cross-env VITE_SAFE_MODE=1 vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "doctor": "node scripts/doctor.js"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "xlsx-js-style": "^1.2.0"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "jsdom": "^24.0.0",
    "vite": "^5.3.1",
    "vitest": "^1.6.0"
  }
}
